{# <form action="{{ item.action }}" method="post" class="search">
	<button type="submit" class="search__button">
		<img src="{{ item.button_icon_src }}" alt="{{ item.button_icon_alt }}" class="search__button-icon" width="24" height="24"/>
	</button>

	<input type="search" name="{{ item.name }}" class="search__input" placeholder="{{ item.placeholder }}" minlength="2" maxlength="500" required/>
</form> #}
