<section class="team">
  <h2 class="team__title section-title">{{ team.title }}</h2>

  <div class="team__list">
    {% for member in team.items %}
		{% include 'team-card.twig' with { member: member } %}
    {% endfor %}
  </div>
</section>
